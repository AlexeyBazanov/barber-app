<script>

    <% flash.each do |key, message| %>

      $(document).ready(function () {
          Swal.fire({
              position: 'top-end',
              icon: '<%= key == 'notice' ? 'success' : 'warning' %>',
              title: '<%= message %>',
              showConfirmButton: false,
              timer: 5500
          })
      });

    <% end %>

</script>